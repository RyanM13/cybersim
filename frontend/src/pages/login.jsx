import { LoginForm } from "@/components/login-form";
import { useState } from "react";

const user = { username: "Ryan", password: "password" };

export default function Login() {
  return <LoginForm />;
}
